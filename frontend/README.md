# Frontend - Laravel 11

This directory will contain the Laravel application.

## Setup (will be implemented in Step 4)

```bash
# Container will handle setup automatically
podman-compose up -d frontend
```
