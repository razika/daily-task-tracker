# Backend - Python FastAPI

This directory will contain the FastAPI application.

## Setup (will be implemented in Step 3)

```bash
# Container will handle setup automatically
podman-compose up -d backend
```
